# ── Core ML ───────────────────────────────────────────────────────
numpy>=1.26.0      # 1.24.3 has no wheels for Python 3.12; updated for compatibility
pandas>=2.1.0      # 2.0.3 lacks Python 3.12 wheels; 2.1+ has full support
scikit-learn>=1.4.0  # 1.3.0 has limited 3.12 support; 1.4+ required
joblib>=1.3.2      # 1.3.1 compatible, but bumped to latest patch
matplotlib>=3.8.0  # 3.7.2 needs building on 3.12; 3.8+ has wheels
seaborn>=0.13.0    # Updated for better 3.12 compatibility
python-dotenv>=1.0.0

# ── Experiment tracking ───────────────────────────────────────────
# mlflow 2.7 requires Flask<3 — pinned here for local dev consistency
mlflow>=2.8.0      # Upgraded from 2.7 for better 3.12 support

# ── Inference API (FastAPI replaces Flask) ────────────────────────
fastapi>=0.115.0   # Upgraded from 0.111.0 for stability
uvicorn[standard]>=0.30.0  # includes httptools, uvloop, websockets
pydantic>=2.7.1    # v2 — used by FastAPI for request/response models

# ── Data schema validation ────────────────────────────────────────
pandera>=0.19.0    # Updated from 0.18.0 for Python 3.12

# ── Model explainability ──────────────────────────────────────────
shap==0.45.0
lime==0.2.0.1

# ── Hyperparameter optimisation ───────────────────────────────────
optuna==3.4.0
optuna-integration[mlflow]==3.4.0

# ── Drift detection & monitoring ──────────────────────────────────
evidently==0.4.16

# ── Azure SDK ─────────────────────────────────────────────────────
azure-ai-ml==1.10.0
azure-identity==1.13.0
azure-storage-blob==12.17.0
azure-keyvault-secrets==4.4.0

# ── Application Insights (optional — log_alert_to_insights.py) ───
opencensus-ext-azure==1.1.9

requests==2.31.0