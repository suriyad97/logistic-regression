# ── Core ML ───────────────────────────────────────────────────────
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
joblib==1.3.1
matplotlib==3.7.2
seaborn==0.12.2
python-dotenv==1.0.0

# ── Experiment tracking ───────────────────────────────────────────
# mlflow 2.7 requires Flask<3 — pinned here for local dev consistency
mlflow==2.7.0

# ── Inference API (FastAPI replaces Flask) ────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0   # includes httptools, uvloop, websockets
pydantic==2.7.1             # v2 — used by FastAPI for request/response models

# ── Data schema validation ────────────────────────────────────────
pandera==0.18.0

# ── Model explainability ──────────────────────────────────────────
shap==0.45.0
lime==0.2.0.1

# ── Hyperparameter optimisation ───────────────────────────────────
optuna==3.4.0
optuna-integration[mlflow]==3.4.0

# ── Drift detection & monitoring ──────────────────────────────────
evidently==0.4.16

# ── Azure SDK ─────────────────────────────────────────────────────
azure-ai-ml==1.10.0
azure-identity==1.13.0
azure-storage-blob==12.17.0
azure-keyvault-secrets==4.4.0

# ── Application Insights (optional — log_alert_to_insights.py) ───
opencensus-ext-azure==1.1.9

requests==2.31.0